const initModel = {
  leftValue: 0,
  leftUnit: 'Celsius',
  rightValue: 32,
  rightUnit: 'Fahrenheit',
  sourceLeft: true, // keep track of which value has changed, to determine which value to convert
};

export default initModel;